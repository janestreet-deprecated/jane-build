(executables
 ((names (jane_build))
  (libraries (core jenga-rules jenga))
  (preprocess (pps (ppx_jane)))
))

(install
 ((section bin)
  (files  ((jane_build.exe as jane-build)))))


(jbuild_version jane_street)
